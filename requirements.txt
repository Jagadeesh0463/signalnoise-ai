# SignalNoise AI — Dependencies
# Install: pip install -r requirements.txt

# ── Core NLP / Signal Detection ──────────────────────────────────────────────
bertopic>=0.16.4
sentence-transformers>=3.0.1        # MiniLM embeddings
umap-learn>=0.5.6
hdbscan>=0.8.38
scikit-learn>=1.5.1                 # CountVectorizer used by BERTopic detector

# ── Privacy Layer ─────────────────────────────────────────────────────────────
presidio-analyzer>=2.2.359
presidio-anonymizer>=2.2.355
spacy>=3.7.5,<3.8.0                 # required by Presidio
# spaCy model — installed directly so Streamlit Cloud doesn't need a post-install step
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# ── Vector Store / Memory ─────────────────────────────────────────────────────
chromadb>=0.5.3

# ── LLM (Groq — portfolio demo only) ─────────────────────────────────────────
groq>=0.9.0

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.36.0

# ── File Handling ─────────────────────────────────────────────────────────────
python-docx>=1.1.2
pdfplumber>=0.11.2
chardet>=5.2.0

# ── Data / Utils ──────────────────────────────────────────────────────────────
pandas>=2.2.2
numpy>=1.26.4
python-dotenv>=1.0.1

# ── Knowledge Graph ───────────────────────────────────────────────────────────
networkx>=3.3

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.2
pytest-cov>=5.0.0
