"""
src/__version__.py

Single source of truth for the package version.
Import from here — never hardcode the version anywhere else.

Usage:
    from src.__version__ import __version__
    print(__version__)   # "1.0.0"
"""

__version__ = "1.0.0"
__version_info__ = (1, 0, 0)
